Plugin for editing files as hex data in vim

The plugin is very simple and uses xxd for converting to hex.

Commands:
:HexToggle
	Toggles the current buffer between normal and hex view

This plugin doesn't have any settings
